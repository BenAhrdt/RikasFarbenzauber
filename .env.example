DJANGO_DEBUG=false
DJANGO_SECRET_KEY=replace-with-a-random-secret-at-least-50-characters-long
DJANGO_ALLOWED_HOSTS=farben.example.org
DJANGO_CSRF_TRUSTED_ORIGINS=https://farben.example.org
DATABASE_PATH=/home/ben/RikasFarbenzauber/data/db.sqlite3
# Trusted public release source. Set empty to disable update checks.
UPDATE_MANIFEST_URL=https://github.com/BenAhrdt/RikasFarbenzauber/releases/latest/download/latest.json
# Enabled by scripts/install.sh only when the systemd update worker is installed.
UPDATE_INSTALL_ENABLED=false
# UPDATE_STATE_DIR=/var/lib/rikas-farbenzauber/updates
